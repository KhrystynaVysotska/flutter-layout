import 'package:flutter/material.dart';

Icon starIcon = Icon(
  Icons.star,
  size: 15.0,
  color: Colors.black.withOpacity(0.6),
);

Icon bookmarkIcon = Icon(
  Icons.bookmark_border,
  size: 20.0,
  color: Colors.black.withOpacity(0.6),
);

Icon arrowBackIcon = const Icon(
  Icons.arrow_back,
  semanticLabel: 'Return',
);
