import 'package:flutter/material.dart';

Widget starIcon = Icon(
  Icons.star,
  size: 15.0,
  color: Colors.black.withOpacity(0.6),
);

Widget bookmarkIcon = Icon(
  Icons.bookmark_border,
  size: 20.0,
  color: Colors.black.withOpacity(0.6),
);

Widget arrowBackIcon = const Icon(
  Icons.arrow_back,
  semanticLabel: 'Return',
);
